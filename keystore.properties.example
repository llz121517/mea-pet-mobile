## Release 签名证书配置（示例）
#
# 复制本文件为根目录的 keystore.properties（已被 .gitignore 忽略，不要提交）：
#   cp keystore.properties.example keystore.properties
# 填入你自己的签名证书信息后，tools/build-release.ps1 / build-release.sh 会用它对
# v7a / v8a / both 三个 release APK 签名并输出到 dist/。
#
# 也可以用 4 个环境变量替代本文件（脚本与 gradle 均读），变量名见注释：
#   KEYSTORE_STORE_FILE      storeFile
#   KEYSTORE_STORE_PASSWORD  storePassword
#   KEYSTORE_KEY_ALIAS       keyAlias
#   KEYSTORE_KEY_PASSWORD    keyPassword

# keystore 文件路径（相对仓库根，或绝对路径）
storeFile=release.keystore
# keystore 主密码
storePassword=your-store-password
# 签名 key 别名
keyAlias=meapet
# 签名 key 密码
keyPassword=your-key-password
